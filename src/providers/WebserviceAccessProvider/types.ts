import * as React from "react";

/**
 * Webservice interface
 */
export interface WebserviceInterface {
    code: string
}

/**
 * WebserviceAccessProvider props
 */
export interface WebserviceAccessProviderProps {
    children: React.ReactNode
}
